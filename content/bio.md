---
# Identity panel content. `highlight` marks the ONE phrase that gets the marigold band —
# it must appear verbatim inside `statement`.
now: "Measuring a portfolio's impact before it exists"   # TODO(update): keep this fresh — it's the most visible line on the site
highlight: "this site is the bridge"
---

I run field experiments on governance and public goods — and build the measurement
tools that help funders and social-sector teams act on evidence. Research on one side,
practice on the other; this site is the bridge.

<!-- TODO(update): Andrés — rewrite in your own voice if this reads too Claude. 2–3 sentences max,
     must contain the `highlight` phrase verbatim. -->
